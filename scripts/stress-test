#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
project_dir="$(cd -- "${script_dir}/.." && pwd)"

exec cargo run --manifest-path "${project_dir}/Cargo.toml" --release --bin stress -- "$@"
